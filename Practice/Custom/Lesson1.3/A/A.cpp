#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(nullptr);

    int A;
    int B;

    cin >> A;
    cin >> B;

    long long suma = (long long)A + B;

    cout << suma << "\n";
    return 0;
}